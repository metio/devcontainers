name: Crystal
on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/crystal.yml
      - languages/crystal/**
  schedule:
    - cron:  25 3 * * MON
env:
  LANGUAGE: crystal
  REPOSITORY: metio/devcontainers
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
      security-events: write
    strategy:
      matrix:
        version: ["latest"]
    steps:
      - id: checkout
        name: Checkout Code
        uses: actions/checkout@v3
      - id: cosign-install
        name: Install Cosign
        uses: sigstore/cosign-installer@main
      - id: syft-install
        name: Install Syft
        uses: anchore/sbom-action/download-syft@v0.12.0
      - id: grype-install
        name: Install Grype
        uses: anchore/scan-action/download-grype@v3
      - id: buildx
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - id: dockerhub_login
        name: Login to Dockerhub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - id: timestamp
        name: Create Timestamp
        run: echo "::set-output name=date::$(date --rfc-3339=seconds)"
      - id: publish
        name: Publish Image
        uses: docker/build-push-action@v3
        with:
          builder: ${{ steps.buildx.outputs.name }}
          context: ./languages/${{ env.LANGUAGE }}/${{ matrix.version }}/
          file: ./languages/${{ env.LANGUAGE }}/${{ matrix.version }}/Containerfile
          push: true
          tags: docker.io/${{ env.REPOSITORY }}-${{ env.LANGUAGE }}:${{ matrix.version }}
          labels: |
            org.opencontainers.image.title=${{ env.LANGUAGE }}
            org.opencontainers.image.description=Image for ${{ env.LANGUAGE }} development
            org.opencontainers.image.licenses=CC0-1.0
            org.opencontainers.image.vendor=metio.wtf
            org.opencontainers.image.url=https://github.com/${{ github.repository }}
            org.opencontainers.image.documentation=https://github.com/${{ github.repository }}
            org.opencontainers.image.source=https://github.com/${{ github.repository }}/languages/${{ env.LANGUAGE }}/${{ matrix.version }}
            org.opencontainers.image.version=${{ matrix.version }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ steps.timestamp.outputs.date }}
          cache-from: type=registry,ref=${{ env.REPOSITORY }}-${{ env.LANGUAGE }}:buildcache
          cache-to: type=registry,ref=${{ env.REPOSITORY }}-${{ env.LANGUAGE }}:buildcache,mode=max
      - id: sign
        name: Sign Image
        run: cosign sign docker.io/${{ env.REPOSITORY }}-${{ env.LANGUAGE }}@${{ steps.publish.outputs.digest }}
        env:
          COSIGN_EXPERIMENTAL: "true"
      - id: sbom
        name: Generate SBOM
        run: syft docker.io/${{ env.REPOSITORY }}-${{ env.LANGUAGE }}@${{ steps.publish.outputs.digest }} -o spdx-json=sbom-spdx.json
      - id: sbom-attest
        name: Attest SBOM
        run: |
          cosign attest \
            --predicate sbom-spdx.json \
            --type spdx \
            docker.io/${{ env.REPOSITORY }}-${{ env.LANGUAGE }}@${{ steps.publish.outputs.digest }}
        env:
          COSIGN_EXPERIMENTAL: "true"
      - id: grype-scan
        name: Scan SBOM with Grype
        run: grype -o sarif --only-fixed sbom:sbom-spdx.json > grype-${{ env.LANGUAGE }}-${{ matrix.version }}.sarif
      - id: hadolint-scan
        name: Scan Containerfile with Hadolint
        uses: hadolint/hadolint-action@v2.1.0
        with:
          dockerfile: ./languages/${{ env.LANGUAGE }}/${{ matrix.version }}/Containerfile
          format: sarif
          output-file: hadolint-${{ env.LANGUAGE }}-${{ matrix.version }}.sarif
          no-fail: true
      - id: fix-sarif
        name: Fix SARIF Reports
        run: |
          jq '.runs[].automationDetails.id |= "grype-${{ env.LANGUAGE }}-${{ matrix.version }}"' grype-${{ env.LANGUAGE }}-${{ matrix.version }}.sarif > grype-fixed.sarif
          jq '.runs[].automationDetails.id |= "hadolint-${{ env.LANGUAGE }}-${{ matrix.version }}"' hadolint-${{ env.LANGUAGE }}-${{ matrix.version }}.sarif > hadolint-fixed.sarif
      - id: grype-upload
        name: Upload Grype SARIF Report
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: grype-fixed.sarif
          category: grype-${{ env.LANGUAGE }}-${{ matrix.version }}
      - id: hadolint-upload
        name: Upload Hadolint SARIF Report
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: hadolint-fixed.sarif
          category: hadolint-${{ env.LANGUAGE }}-${{ matrix.version }}
      - id: documentation
        name: Docker Hub Description
        uses: peter-evans/dockerhub-description@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: ${{ env.REPOSITORY }}-${{ env.LANGUAGE }}
          short-description: Devcontainer for Crystal with Crystalline
          readme-filepath: ./languages/${{ env.LANGUAGE }}/README.md
